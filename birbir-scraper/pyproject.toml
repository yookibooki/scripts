[project]
name = "birbir-scraper"
version = "0.1.0"
description = "Scrape all products from Birbir.uz — normalized schema + detail enrichment"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]

